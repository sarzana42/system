<div class="well">
    <%= form_for(@deliverymethod, html: { class: "form-horizontal" }) do |dm| %>
    <fieldset>
      <legend>登録・編集</legend>
        <% if @deliverymethod.errors.present? %>
           <%= alert %>
           <% @deliverymethod.errors.full_messages.each do |full_message| %>
             <%= full_message %>
          <% end %>
        <% end %>
      <div class="form-group">
          <label for="name" class="col col-xs-12 col-sm-4 col-md-3 control-label">納品方法</label>
          <div class="col col-xs-5 col-sm-3 col-md-3">
            <%= dm.text_field :name, class: 'form-control', placeholder: '来店' %>
          </div>
          <div class="col-xs-offset-2 col-sm-offset-2 col-md-offset-3"></div>
      </div>
      <div class="form-group">
          <label for="remark" class="col col-xs-12 col-sm-4 col-md-3 control-label">備考</label>
          <div class="col col-xs-5 col-sm-3 col-md-3">
            <%= dm.text_area :remark, class: 'form-control', placeholder: '' %>
          </div>
          <div class="col-xs-offset-2 col-sm-offset-2 col-md-offset-3"></div>
      </div>
      
        <div class="form-group">
          <div class="col col-xs-9 col-xs-offset-3 col-sm-8 col-sm-offset-4 col-md-9 col-md-offset-3">
            <%= dm.submit class: "btn btn-success btn-lg" %>
          </div>
        </div>
      
    </fieldset>
    <% end %>
  </form>
</div>