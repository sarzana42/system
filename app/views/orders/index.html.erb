<section class="content-header">
  <h1>注文一覧</h1>
</section>

<%= link_to "新規登録", new_order_path(@order) %>
<div class="box-body table-responsive no-padding">
      <table class="table table-hover">
        <thead>
          <tr>
            <th>商品型番</th>
            <th>商品名</th>
            <th>編集</th>
            <th>確認</th>
            <th>削除</th>
            <th>複製</th>
          </tr>
        </thead>
        <% @orders.each do |order| %>
        <tr>
          <td>
            <%= order.id %>
          </td>
          <td><%= order.customer.name1 %></td>
          <td><%= link_to "編集", edit_order_path(order) %></td>
          <td><%= link_to "確認", order_path(order) %></td>
          <td><%= link_to "削除", order, method: :delete, data: {confirm: '削除してもいいですか？'} %></td>
          <td><%= link_to "複製", order_path(order) %></td>
        </tr>
        <% end %>
      </table>
    </div>
  </div><!-- /.box -->
</section>
